# MarkLeaf 阶段 6 报告

## 范围

阶段 6 完成 GFM 表格、任务列表和图片工作流。当前图片策略直接保存并读取本机绝对路径，不再为文档创建 `.assets` 资源目录。

## 已完成

- GFM 表格：插入表格、增删行列、删除表格、左/中/右对齐、表头渲染和 Tiptap 原生 Tab 导航。
- 任务列表：菜单切换、复选框交互、嵌套、Enter/Backspace 列表行为和 Markdown 往返。
- 图片粘贴与拖入：支持 PNG、JPEG、GIF、WebP、BMP，单文件限制 50 MiB；拒绝 SVG、扩展名伪装和不匹配的文件签名。
- 从文件选择、拖入或剪贴板文件列表插入的图片不复制，Markdown 保存原文件的绝对路径。
- 剪贴板位图写入应用安装目录的 `Cache/ClipboardImages`，Markdown 保存缓存文件的绝对路径。
- 打开旧文档时，本机相对图片路径会按文档目录解析并规范化为绝对路径。
- 加载时检测缺失的本机图片，弹窗列出文件名，并允许用户逐一查找替换；替换后文档保持未保存状态。
- 已移除“文件 > 清理未引用资源”，应用不会删除外部图片或剪贴板缓存图片。
- WebView2 仍通过隔离的 `assets.local` 请求代理读取图片，但代理目标来自 Markdown 中的完整本机路径。

## 主要文件

- `src/EditorWeb/src/editor.ts`：表格、任务列表、图片渲染、绝对路径代理 URL 和命令状态。
- `src/EditorWeb/src/main.ts`：图片粘贴/拖放协议。
- `src/MarkLeaf.App/Documents/ImageAssetService.cs`：图片验证、绝对路径规范化、剪贴板缓存和缺失图片分析。
- `src/MarkLeaf.App/UI/MainForm.Documents.cs`：图片插入、缺失提示与查找替换流程。
- `src/MarkLeaf.App/Native/NativeMenuService.cs`：表格、任务列表、图片命令及文件菜单。
- `src/EditorWeb/tests/roundtrip.test.ts`、`tests/MarkLeaf.Tests/ImageAssetServiceTests.cs`：阶段 6 回归测试。

## 构建与测试

- EditorWeb 测试：50/50 通过。
- EditorWeb 生产构建：通过。
- .NET 测试：60/60 通过。
- 解决方案构建：0 警告、0 错误。
- 真实 WebView2 集成冒烟：生命周期进入 `Ready`，文档加载、命令执行和快照请求响应均成功。

## 已知边界

- MVP 不支持合并单元格、多段落单元格或 Excel 级交互。
- 表格仅剩最后一行或最后一列时，删除命令会被 Tiptap 拒绝以避免损坏；使用“删除表格”移除整个表格。
- 绝对路径会降低 Markdown 文档跨计算机或跨目录移动时的可移植性；移动图片后会在下次打开时触发缺失图片替换流程。
- 剪贴板缓存位于安装目录；若安装目录不可写，粘贴位图会失败并显示错误，不会要求用户另存。
- 当前不自动清理剪贴板缓存，避免误删仍被文档引用的图片。

## 人工验收反馈修复

- 修复图片粘贴提示成功但编辑区不显示：图片插入改为带请求 ID 的命令，宿主等待编辑器返回实际插入结果后才提示成功。
- 宿主拦截 `assets.local` 图片请求，按 Markdown 中的绝对路径读取支持格式的本机图片。
- 修复资源管理器拖放显示禁用符号：启用 WebView2 外部拖放，并使用 `postMessageWithAdditionalObjects`/`CoreWebView2File` 获取真实文件路径，不再依赖浏览器中不可用的 `File.path`。
- 剪贴板粘贴会优先识别文件列表；其中支持的图片文件按图片导入，随后才处理位图或文本。
- “段落”菜单新增“插入图片...”，支持一次选择多张图片。
- 拖放坐标在 WebView2 页面中由 ProseMirror `posAtCoords` 解析，图片插入鼠标释放位置而非旧焦点位置。
- 关闭、放弃保存或正常退出均不会删除外部图片或剪贴板缓存图片。
- 已移除“文件 > 清理未引用资源”菜单命令。
- 图片节点选中后显示四个角的缩放手柄；拖动时保持宽高比，并在释放鼠标时提交一次可撤销的尺寸变更。
- “段落”菜单新增“顺时针旋转图片”，仅在选中图片节点时启用；每次旋转 90 度，支持撤销与重做。
- 图片宽度、高度和旋转角度通过保留的 `markleaf:` 图片标题元数据写入 Markdown，普通图片标题保持不变，保存并重新打开后可恢复。

## 本轮回归结果

- EditorWeb 测试：50/50 通过。
- EditorWeb 生产构建：通过。
- .NET 测试：60/60 通过。
- 解决方案构建：0 警告、0 错误。

## 请开发者检查

1. 插入表格，使用 Tab 在单元格间移动，并测试增删行列、三种对齐和删除表格。
2. 创建任务列表，点击复选框，测试嵌套以及 Enter/Backspace 行为，保存后重新打开。
3. 从剪贴板粘贴图片，并拖入支持与不支持的文件；确认不支持文件不会进入文档。
4. 从磁盘选择或拖入图片后保存，确认 Markdown 中写入原图片的绝对路径，且没有创建 `.assets` 目录。
5. 粘贴剪贴板位图，确认文件写入应用目录的 `Cache/ClipboardImages`，Markdown 中写入该缓存文件的绝对路径。
6. 使用“另存为”保存到其他名称或目录，确认图片路径保持为原绝对路径且不复制图片。
7. 关闭并重新打开包含图片、表格和任务列表的文档，确认往返和渲染正确。
8. 选中图片，确认四角出现缩放手柄；分别拖动四个角，确认图片等比例缩放且撤销/重做正常。
9. 选中图片后执行“段落 > 顺时针旋转图片”，确认菜单仅在图片选中时启用；连续旋转四次应回到原方向。
10. 调整图片大小并旋转后保存、关闭并重新打开，确认尺寸和角度均被保留。
11. 删除或移动被引用图片后重新打开文档，确认弹窗列出缺失文件名；选择替换文件后确认图片恢复，并保存更新后的绝对路径。

本阶段已停止，等待开发者检查和确认。未开始阶段 7。
