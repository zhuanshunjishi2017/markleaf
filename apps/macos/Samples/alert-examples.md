# GitHub 风格提示框示例

下面的五种提示框会在可视化模式和导出 HTML 中保持语义结构。

> [!NOTE]
> 备注：适合补充背景信息，不会打断主要阅读流程。

> [!TIP]
> 提示：可以用 `Command+Option+U` 快速切换源码模式。

> [!IMPORTANT]
> 重要：这里强调用户必须理解的关键信息。

> [!WARNING]
> 警告：这里提醒可能导致数据丢失或构建失败的操作。

> [!CAUTION]
> 注意：这里标注破坏性后果最严重的场景。

## 多段落保留

> [!TIP]
> 提示框可以包含多个段落。
>
> 1. 第一项保持编号。
> 2. 第二项不会丢失样式。
>
> ```swift
> let supported = true
> ```
